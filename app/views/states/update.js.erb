function refreshReadState(new_class) {

  const influencerContainer = document.getElementById("<%= @state.id %>readstate");
  influencerContainer.classList.toggle(new_class);
}

refreshReadState( "<%= @new_class %>");
